# Галерея пользовательских шаблонов SmartTable

## Описание

Галерея пользовательских шаблонов позволяет пользователям:
- 📥 **Импортировать** шаблоны таблиц
- 📋 **Применять** пользовательские шаблоны для создания новых таблиц
- 🔍 **Искать** шаблоны по названию и описанию
- 📤 **Экспортировать** созданные шаблоны для обмена с другими пользователями
- 🗑️ **Управлять** установленными шаблонами

## Структура папки `user_templates`

```
user_templates/
├── templates/              # Файлы шаблонов (JSON)
│   ├── мой_шаблон.json
│   ├── кастомный_отчет.json
│   └── ...
├── metadata/              # Метаданные шаблонов
│   ├── мой_шаблон.json
│   ├── кастомный_отчет.json
│   └── ...
└── thumbnails/           # Превью шаблонов (опционально)
```

## Формат файла шаблона

### Структура JSON-файла шаблона:

```json
{
  "name": "Мой шаблон",
  "description": "Описание шаблона",
  "created_at": "2026-02-09T00:00:00.000000",
  "modified_at": "2026-02-09T00:00:00.000000",
  "source_range": null,
  "fields": [
    {
      "name": "Поле 1",
      "column_index": 0,
      "pattern": "text",
      "format_string": "",
      "is_key_field": false,
      "validation_rules": []
    },
    {
      "name": "Поле 2",
      "column_index": 1,
      "pattern": "number",
      "format_string": "",
      "is_key_field": false,
      "validation_rules": []
    }
  ],
  "logic_rules": [],
  "settings": {
    "auto_detect_patterns": true,
    "preserve_formulas": true,
    "include_headers": true,
    "skip_empty_rows": true
  }
}
```

## Параметры шаблона

### Основные параметры

| Параметр | Тип | Описание |
|----------|-----|---------|
| `name` | string | Название шаблона |
| `description` | string | Описание и назначение шаблона |
| `created_at` | string | Дата создания (ISO format) |
| `modified_at` | string | Дата последнего изменения |
| `source_range` | null/string | Исходный диапазон ячеек |

### Параметры полей (`fields`)

| Параметр | Тип | Описание |
|----------|-----|---------|
| `name` | string | Название поля/колонки |
| `column_index` | number | Индекс колонки (начиная с 0) |
| `pattern` | string | Тип данных поля |
| `format_string` | string | Формат вывода данных |
| `is_key_field` | boolean | Является ли это ключевым полем |
| `validation_rules` | array | Правила валидации данных |

### Типы данных (`pattern`)

| Тип | Описание | Примеры |
|-----|---------|---------|
| `text` | Обычный текст | "Иван", "Петров" |
| `number` | Число | 123, 45.67 |
| `currency` | Денежное значение | 1000₽, $999 |
| `date` | Дата | 2026-02-09 |
| `email` | Email адрес | user@example.com |
| `phone` | Телефонный номер | +7 (999) 123-45-67 |
| `url` | Веб-адрес | https://example.com |
| `percent` | Процент | 50% |

### Параметры настроек (`settings`)

| Параметр | Описание |
|----------|---------|
| `auto_detect_patterns` | Автоматически определять типы данных |
| `preserve_formulas` | Сохранять формулы при импорте |
| `include_headers` | Включать строку заголовков |
| `skip_empty_rows` | Пропускать пустые строки |

## Примеры пользовательских шаблонов

### 1. Шаблон "Контакты компании"
**Использование:** для ведения базы контактов сотрудников и партнеров

```json
{
  "name": "Контакты компании",
  "description": "Шаблон для управления контактами сотрудников и деловых партнеров",
  "fields": [
    {"name": "Фамилия", "column_index": 0, "pattern": "text"},
    {"name": "Имя", "column_index": 1, "pattern": "text"},
    {"name": "Email", "column_index": 2, "pattern": "email"},
    {"name": "Телефон", "column_index": 3, "pattern": "phone"},
    {"name": "Должность", "column_index": 4, "pattern": "text"},
    {"name": "Компания", "column_index": 5, "pattern": "text"}
  ],
  "settings": {
    "auto_detect_patterns": true,
    "preserve_formulas": true,
    "include_headers": true,
    "skip_empty_rows": true
  }
}
```

### 2. Шаблон "Финансовый отчет"
**Использование:** для отслеживания доходов, расходов и прибыли

```json
{
  "name": "Финансовый отчет",
  "description": "Шаблон для финансового анализа и отчетности",
  "fields": [
    {"name": "Дата", "column_index": 0, "pattern": "date"},
    {"name": "Статья", "column_index": 1, "pattern": "text"},
    {"name": "Категория", "column_index": 2, "pattern": "text"},
    {"name": "Доход", "column_index": 3, "pattern": "currency"},
    {"name": "Расход", "column_index": 4, "pattern": "currency"},
    {"name": "Баланс", "column_index": 5, "pattern": "currency"}
  ],
  "settings": {
    "auto_detect_patterns": true,
    "preserve_formulas": true,
    "include_headers": true,
    "skip_empty_rows": false
  }
}
```

### 3. Шаблон "Инвентаризация"
**Использование:** для учета товаров на складе с отслеживанием количества

```json
{
  "name": "Инвентаризация",
  "description": "Шаблон для учета товаров на складе",
  "fields": [
    {"name": "Артикул", "column_index": 0, "pattern": "text", "is_key_field": true},
    {"name": "Название", "column_index": 1, "pattern": "text"},
    {"name": "Категория", "column_index": 2, "pattern": "text"},
    {"name": "Количество", "column_index": 3, "pattern": "number"},
    {"name": "Единица", "column_index": 4, "pattern": "text"},
    {"name": "Цена закупки", "column_index": 5, "pattern": "currency"},
    {"name": "Цена продажи", "column_index": 6, "pattern": "currency"},
    {"name": "Дата обновления", "column_index": 7, "pattern": "date"}
  ]
}
```

## Использование галереи

### Открыть галерею шаблонов:
1. **Файл** → **Галерея шаблонов...**
2. Или используйте горячую клавишу (если добавлена в настройках)

### Применить шаблон:
1. Найдите нужный шаблон в галерее
2. Нажмите кнопку **"Применить"** на карточке
3. Новая таблица будет создана со структурой из шаблона

### Импортировать шаблон:
1. Нажмите **"📥 Импортировать"**
2. Выберите JSON-файл шаблона
3. Шаблон будет добавлен в вашу галерею

### Поиск шаблонов:
1. Введите название или ключевые слова в поле поиска
2. Результаты будут отфильтрованы в реальном времени

### Создать новый шаблон:
1. Оформите данные в нужном формате в таблице
2. **Инструменты** → **Создать шаблон из выделения**
3. Укажите название и описание
4. Определите типы данных для каждого поля
5. Нажмите **"Создать"**

### Отредактировать шаблон:
1. Откройте шаблон в галерее
2. Нажмите **"✏️ Редактировать"**
3. Измените параметры по необходимости
4. Сохраните изменения

### Экспортировать шаблон:
1. Выберите шаблон
2. Нажмите **"📤 Экспортировать"**
3. Файл JSON будет сохранен в выбранную папку

## Создание собственного шаблона

### Способ 1: Через UI приложения

1. Подготовьте данные в таблице
2. Выделите диапазон данных
3. Нажмите кнопку создания нового шаблона
4. Заполните форму с названием и описанием
5. Определите типы данных для каждого поля
6. Сохраните

### Способ 2: Скопировать существующий

1. Откройте файл шаблона из папки `templates/`
2. Измените:
   - `name` - новое название
   - `description` - новое описание
   - Укажите названия полей
3. Сохраните с новым именем (например, `новый_шаблон.json`)

### Способ 3: Создать с нуля

1. Создайте новый JSON-файл по шаблону выше
2. Определите структуру ваших данных (колонки)
3. Для каждого поля укажите название и тип данных
4. Добавьте описание шаблона
5. Сохраните в папке `templates/`
6. Создайте метаданные в папке `metadata/` (копия файла)

## Метаданные шаблона

Файл метаданных содержит копию основного шаблона для быстрого отображения информации в галерее (без загрузки основного файла).

Структура идентична основному файлу шаблона.

## Советы по созданию шаблонов

### 1. Выбирайте понятные названия
❌ Плохо: `template1.json`, `test.json`
✅ Хорошо: `контакты_клиентов.json`, `еженедельный_отчет.json`

### 2. Добавляйте описание
Описание должно быть кратким и информативным:
```
"Шаблон для управления данными о клиентах с контактной информацией"
```

### 3. Правильно определяйте типы данных
Это помогает при валидации и форматировании:
- Email → используйте `email` тип
- Деньги → используйте `currency`
- Даты → используйте `date`

### 4. Организуйте поля логически
Расположите поля в естественном порядке ввода:
- Сначала идентификаторы (ID, артикул)
- Потом основная информация (название, описание)
- Потом детали (цена, дата, контакты)

### 5. Используйте метаданные
Они помогают быстро найти нужный шаблон в галерее.

## Обмен шаблонами

### Общий доступ:
1. Экспортируйте нужный шаблон
2. Отправьте JSON-файл коллеге
3. Коллега импортирует файл через галерею

### Версионирование:
Если вы часто обновляете шаблон, добавляйте номер версии в название:
```
мой_отчет_v1.json
мой_отчет_v2.json
мой_отчет_v3.json
```

## Структура папок (подробно)

### templates/
Хранит основные файлы шаблонов в формате JSON.
- Один файл = один шаблон
- Название файла должно совпадать с полем `name` в JSON

Пример:
```
Контакты.json
Продажи.json
Инвентаризация.json
```

### metadata/
Содержит копии метаданных шаблонов для быстрого доступа.
- Используется галереей для отображения списка
- Может содержать дополнительные поля для фильтрации/поиска

### thumbnails/
Хранит миниатюры (превью) шаблонов.
- Опционально (используется для визуального отображения)
- Форматы: PNG, JPG
- Размер рекомендуемый: 240x180 пикселей

## Решение проблем

### Шаблон не загружается
1. Проверьте синтаксис JSON (используйте [JSON validator](https://jsonlint.com/))
2. Убедитесь, что все обязательные поля заполнены
3. Проверьте кодировку файла (должна быть UTF-8)

### Поля имеют неправильный тип
1. Проверьте значение `pattern` для каждого поля
2. Используйте только указанные типы данных
3. При необходимости используйте `text` как универсальный тип

### Шаблон не появляется в галерее
1. Проверьте, что файл находится в папке `templates/`
2. Перезагрузите приложение
3. Проверьте файл на ошибки синтаксиса

## Спецификация версии

- **Версия:** 1.0
- **Дата:** Февраль 2026
- **Совместимость:** SmartTable v1.0+

---

**Нужна помощь?** Ознакомьтесь с примерами в папке `templates/` или обратитесь к основной документации.
