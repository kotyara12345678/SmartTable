# Галерея пользовательских тем SmartTable

## Описание

Галерея позволяет пользователям:
- 📥 **Импортировать** темы оформления
- 🎨 **Применять** пользовательские темы
- 🔍 **Искать** темы по названию и описанию
- 📤 **Экспортировать** созданные темы
- 🗑️ **Управлять** установленными темами

## Структура папки `user_themes`

```
user_themes/
├── themes/              # Файлы тем (JSON)
│   ├── ocean_sunset.json
│   ├── minimalist.json
│   └── night_code.json
├── metadata/            # Метаданные тем
│   ├── ocean_sunset.json
│   ├── minimalist.json
│   └── night_code.json
└── thumbnails/         # Превью тем (опционально)
```

## Формат файла темы

### Структура JSON-файла темы:

```json
{
  "metadata": {
    "name": "Название темы",
    "description": "Описание темы",
    "author": "Автор",
    "version": "1.0",
    "category": "custom",
    "tags": ["синий", "оранжевый"],
    "preview_color": "#FF6B6B"
  },
  "theme": {
    "light": {
      "primary": "#FF6B6B",
      "primary_light": "#FFB3B3",
      "primary_dark": "#CC5555",
      "secondary": "#4ECDC4",
      "background": "#F7F9FB",
      "surface": "#FFFFFF",
      "text_primary": "#2C3E50",
      "text_secondary": "#7F8C8D",
      "border": "#E1E8EF",
      "success": "#2ECC71",
      "warning": "#F39C12",
      "error": "#E74C3C"
    },
    "dark": {
      "primary": "#FF6B6B",
      "primary_light": "#FF8E8E",
      "primary_dark": "#CC5555",
      "secondary": "#4ECDC4",
      "background": "#0D1117",
      "surface": "#161B22",
      "text_primary": "#FFFFFF",
      "text_secondary": "#C9D1D9",
      "border": "#404040",
      "success": "#3FB950",
      "warning": "#D29922",
      "error": "#F85149"
    }
  }
}
```

## Параметры метаданных

| Параметр | Тип | Описание |
|----------|-----|---------|
| `name` | string | Название темы |
| `description` | string | Описание темы |
| `author` | string | Автор темы |
| `version` | string | Версия темы (опционально) |
| `category` | string | Категория: `custom`, `light`, `dark`, `system` |
| `tags` | array | Массив тегов для поиска |
| `preview_color` | string | Основной цвет темы (HEX) |

## Параметры цветов

### Светлая тема (`light`):
- `primary` - Основной цвет (акцент)
- `primary_light` - Светлая версия основного цвета
- `primary_dark` - Тёмная версия основного цвета
- `secondary` - Вторичный цвет
- `background` - Фон приложения
- `surface` - Фон элементов UI
- `text_primary` - Основной цвет текста
- `text_secondary` - Вторичный цвет текста
- `border` - Цвет границ
- `success` - Цвет успеха (зелёный)
- `warning` - Цвет предупреждения (жёлтый)
- `error` - Цвет ошибки (красный)

### Тёмная тема (`dark`):
Те же параметры, но с цветами для тёмного режима

## Использование галереи

### Открыть галерею:
1. **Файл** → **Галерея тем...**
2. Или используйте горячую клавишу `Ctrl+Shift+T` (если добавлена)

### Применить тему:
1. Найдите тему в галерее
2. Нажмите кнопку **"Применить"** на карточке темы
3. Тема будет немедленно применена

### Импортировать тему:
1. Нажмите **"📥 Импортировать"**
2. Выберите JSON-файл темы
3. Тема будет добавлена в галерею

### Поиск тем:
1. Введите название или описание в поле поиска
2. Результаты будут отфильтрованы в реальном времени

## Создание собственной темы

### Способ 1: Скопировать существующую

1. Откройте файл темы из папки `user_themes/themes/`
2. Измените параметры:
   - `metadata.name` - новое название
   - `metadata.description` - новое описание
   - Цвета в разделе `theme`
3. Сохраните с новым названием (например, `my_theme.json`)

### Способ 2: Создать с нуля

1. Создайте новый JSON-файл по шаблону выше
2. Используйте [HEX коды цветов](https://htmlcolorcodes.com/)
3. Сохраните в папке `user_themes/themes/`
4. Создайте соответствующий файл метаданных в `user_themes/metadata/`

## Примеры тем

### 1. Морской закат (`ocean_sunset.json`)
- **Цвета:** Коралл (#FF6B6B) + Бирюза (#4ECDC4)
- **Стиль:** Теплый, живой, энергичный
- **Использование:** Творческие проекты, презентации

### 2. Минималист (`minimalist.json`)
- **Цвета:** Зелень (#52B788) + Серый (#E0E0E0)
- **Стиль:** Чистый, профессиональный, простой
- **Использование:** Деловые документы, отчёты

### 3. Ночной код (`night_code.json`)
- **Цвета:** Фиолет (#6E7DEE) + Розовый (#F72E6D)
- **Стиль:** Тёмный, современный, для программистов
- **Использование:** Работа с данными, анализ

## API для программистов

### Использование ThemeGalleryManager:

```python
from pysheets.src.ui.gallery import ThemeGalleryManager

# Инициализация менеджера
manager = ThemeGalleryManager()

# Получить все темы
all_themes = manager.get_all_themes()

# Получить тему по ID
theme = manager.get_theme('ocean_sunset')

# Поиск тем
results = manager.search_themes('закат')

# Получить темы по категории
custom_themes = manager.get_themes_by_category('custom')

# Импортировать тему
manager.import_theme('path/to/theme.json')

# Экспортировать тему
manager.export_theme('ocean_sunset', 'export/ocean_sunset.json')

# Удалить тему
manager.delete_theme('ocean_sunset')
```

## Будущие функции

- [ ] Скачивание тем с сервера
- [ ] Загрузка созданных тем на сервер
- [ ] Просмотр превью перед применением
- [ ] Редактор тем (GUI)
- [ ] Синхронизация тем между устройствами
- [ ] Рейтинг и отзывы на темы

## Поддержка

Вопросы и предложения приветствуются! 
Откройте issue на GitHub или свяжитесь с командой разработки.

---

*Создано для SmartTable v1.0*
