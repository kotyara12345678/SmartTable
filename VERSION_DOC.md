# SmartTable — История версий и новые функции

## Версия 2.0.0 (Февраль 2025)

### 🎉 Крупное обновление с модульной архитектурой

---

## 📋 Содержание

- [Новые возможности](#-новые-возможности)
- [Архитектурные изменения](#-архитектурные-изменения)
- [ИИ Функции](#-ии-функции)
- [Диаграммы и визуализация](#-диаграммы-и-визуализация)
- [Формулы и вычисления](#-формулы-и-вычисления)
- [UI Компоненты](#-ui-компоненты)
- [Исправления и улучшения](#-исправления-и-улучшения)

---

## 🚀 Новые возможности

### ✨ Впервые добавлены

#### 1. **ИИ Помощник с двумя режимами работы**
- **Assistant Mode** — быстрые ответы на простые запросы
- **Agent Mode** — многошаговое выполнение сложных задач
- **Автоматическое определение режима** на основе сложности запроса
- **Переключение между режимами** по рекомендации ИИ или запросу пользователя

#### 2. **Система диаграмм**
- **10 типов диаграмм**: столбчатые, линейные, круговые, полярные, scatter, bubble
- **Создание из выделенного диапазона** ячеек
- **Экспорт диаграмм** в изображения
- **Настройка внешнего вида**: цвета, заголовки, легенды
- **Интерактивная панель управления** диаграммами

#### 3. **Undo/Redo система**
- **История до 50 действий**
- **Отмена любых изменений**: данные, форматирование, формулы
- **Возврат отмененных действий** (Redo)
- **Автосохранение** после каждого изменения

#### 4. **Расширенная система формул**
- **60+ встроенных функций**
- **ИИ формула** `=AI()` для запросов к искусственному интеллекту
- **Автокомплит** с описанием синтаксиса
- **Вычисление в реальном времени**

#### 5. **Модульная архитектура UI**
- **Компонентный подход** к построению интерфейса
- **Разделение CSS** на модульные файлы
- **HTML шаблоны** для каждого виджета
- **TypeScript классы** для каждого компонента

---

## 🏗 Архитектурные изменения

### Новая структура проекта

```
sheets/src/electron/src/
├── ui/
│   ├── components/           # UI компоненты
│   │   ├── TopBarComponent.ts
│   │   └── RibbonComponent.ts
│   ├── core/                 # Ядро системы
│   │   ├── ai/
│   │   │   ├── ai-service.ts      # ИИ сервис
│   │   │   └── ai-agent.ts        # ИИ агент
│   │   ├── formulas/
│   │   │   └── formulas.ts        # Движок формул
│   │   ├── component.ts           # Базовый класс компонента
│   │   ├── widget.ts              # Базовый класс виджета
│   │   ├── widget-loader.ts       # Загрузчик виджетов
│   │   └── ipc-handlers.ts        # IPC обработчики
│   ├── styles/               # CSS модули
│   │   ├── variables.css
│   │   ├── top-bar.css
│   │   ├── ribbon.css
│   │   ├── formula-bar.css
│   │   ├── spreadsheet.css
│   │   ├── ai-panel.css
│   │   ├── charts.css
│   │   └── diagrams.css
│   ├── templates/            # HTML шаблоны
│   │   ├── top-bar.html
│   │   ├── ribbon.html
│   │   ├── formula-bar.html
│   │   ├── spreadsheet.html
│   │   ├── ai-panel.html
│   │   └── context-menu.html
│   └── widgets/              # Виджеты
│       ├── charts/
│       │   ├── ChartsWidget.ts
│       │   └── charts.html
│       └── formulabar/
│           └── formulas-renderer.ts
├── diagrams/                 # Диаграммы
│   └── charts.ts
├── app.ts                    # Главный файл приложения
├── main.ts                   # Electron main процесс
└── preload.ts                # Preload скрипт
```

### Рефакторинг

#### main.ts
- **Очищен от бизнес-логики**
- **Только Electron процесс**: создание окна, меню, жизненный цикл
- **IPC обработчики вынесены** в отдельный модуль
- **AI логика перемещена** в `core/ai/`

#### AI Service
- **Выделен в отдельный класс** `AIService`
- **Инкапсуляция API ключей**
- **Системный промт** в константе
- **Метод `chat()`** для отправки запросов

#### IPC Handlers
- **Централизованная регистрация** обработчиков
- **Типизированные запросы и ответы**
- **Обработка ошибок**

---

## 🤖 ИИ Функции

### AI Service

**Файл**: `ui/core/ai/ai-service.ts`

#### Возможности
- **Интеграция с OpenRouter API**
- **Поддержка нескольких API ключей** (автоматическое переключение)
- **Модель DeepSeek Chat**
- **Контекстная память** (история чата)
- **Парсинг JSON ответов**
- **Извлечение планов выполнения**

#### Интерфейсы

```typescript
interface AIRequest {
  message: string;
  tableContext?: string;
  mode: 'assistant' | 'agent';
  history?: Array<{role: 'user' | 'assistant', content: string}>;
}

interface AIResponse {
  success: boolean;
  content?: string;
  executionPlan?: any[];
  suggestModeSwitch?: string;
  error?: string;
}
```

### AI Agent

**Файл**: `ui/core/ai/ai-agent.ts`

#### Функции
- **Определение режима** на основе запроса
- **Генерация планов** для сложных задач
- **Быстрые ответы** для простых запросов
- **Предложения действий** (quick replies)

#### Системный промт

Включает:
- **Правила переключения режимов**
- **Формат ответов** (JSON для команд, текст для ответов)
- **Доступные действия** (set_cell, set_formula, create_chart, etc.)
- **Правила форматирования** (цвета, стили)
- **Примеры использования**

### Команды ИИ

| Команда | Описание | Параметры |
|---------|----------|-----------|
| `set_cell` | Установить значение | column, row, value |
| `set_cell_color` | Покрасить ячейку | column, row, bg_color |
| `color_column` | Покрасить столбец | column, bg_color |
| `color_row` | Покрасить строку | row, bg_color |
| `set_formula` | Вставить формулу | column, row, formula |
| `fill_table` | Заполнить таблицу | data (2D array) |
| `clear_cell` | Очистить ячейку | column, row |
| `clear_column` | Очистить столбец | column |
| `clear_row` | Очистить строку | row |
| `create_chart` | Создать диаграмму | type, title, data |

---

## 📊 Диаграммы и визуализация

### ChartsWidget

**Файл**: `ui/widgets/charts/ChartsWidget.ts`

#### Типы диаграмм

```typescript
type ChartType = 
  | 'bar'           // Столбчатая
  | 'horizontalBar' // Горизонтальная столбчатая
  | 'line'          // Линейная
  | 'pie'           // Круговая
  | 'doughnut'      // Кольцевая
  | 'area'          // Областная
  | 'radar'         // Лепестковая
  | 'scatter'       // Точечная
  | 'bubble'        // Пузырьковая
  | 'polarArea';    // Полярная область
```

#### Методы

- **`createChartFromRange(data, type, title)`** — создать из данных
- **`render()`** — отрисовка на Canvas
- **`exportChart()`** — экспорт в PNG
- **`changeChartType()`** — смена типа
- **`close()`** — закрыть панель
- **`show()` / `hide()`** — показать/скрыть

#### Отрисовка

- **Canvas API** для рендеринга
- **Автоматический выбор цветов**
- **Адаптивные размеры**
- **Подписи осей и легенда**

### Диаграммы (diagrams/charts.ts)

#### Вспомогательные функции
- **`drawBarChart()`** — столбчатые диаграммы
- **`drawLineChart()`** — линейные графики
- **`drawPieChart()`** — круговые диаграммы
- **`drawAreaChart()`** — областные диаграммы

---

## 🧮 Формулы и вычисления

### Формулы (core/formulas/formulas.ts)

#### Категории формул

##### Математические (13)
- `SUM` — сумма
- `AVERAGE` — среднее
- `MIN` / `MAX` — мин/макс
- `COUNT` / `COUNTA` — количество
- `PRODUCT` — произведение
- `ROUND` — округление
- `ABS` — модуль
- `POWER` — степень
- `SQRT` — корень

##### Логические (5)
- `IF` — условие
- `AND` / `OR` / `NOT` — логические операторы
- `IFERROR` — обработка ошибок

##### Текстовые (12)
- `CONCATENATE` / `CONCAT` — сцепление
- `LEN` — длина
- `UPPER` / `LOWER` / `PROPER` — регистр
- `TRIM` — удаление пробелов
- `LEFT` / `RIGHT` / `MID` — извлечение
- `SUBSTITUTE` — замена

##### Дата и время (8)
- `TODAY` / `NOW` — текущая дата/время
- `DAY` / `MONTH` / `YEAR` — компоненты даты
- `HOUR` / `MINUTE` / `SECOND` — компоненты времени

##### Поиск и ссылки (4)
- `VLOOKUP` / `HLOOKUP` — вертикальный/горизонтальный поиск
- `INDEX` — значение по индексу
- `MATCH` — позиция

##### Финансовые (3)
- `PMT` — платеж по кредиту
- `FV` — будущая стоимость
- `PV` — текущая стоимость

##### ИИ (1)
- `AI` — ИИ запрос к таблице

#### Вычисление

```typescript
export function evaluateFormula(
  formula: string, 
  getData: (cell: string) => string
): FormulaResult
```

**Возвращает**:
- `value` — результат вычисления
- `error` — сообщение об ошибке (если есть)

---

## 🎨 UI Компоненты

### TopBarComponent

**Файл**: `ui/components/TopBarComponent.ts`

#### Функции
- Отображение верхней панели
- Переключение вкладок меню
- Кнопка открытия ИИ помощника
- Информация о файле

#### События
- `ai-panel-open` — открытие ИИ панели
- Смена активной вкладки

### RibbonComponent

**Файл**: `ui/components/RibbonComponent.ts`

#### Группы инструментов
- **Буфер обмена**: Вставить, Вырезать, Копировать
- **Шрифт**: семейство, размер, начертание, цвет
- **Выравнивание**: лево, центр, право, перенос
- **Число**: формат, разрядность
- **Ячейки**: границы, объединение
- **Формулы**: автосумма, функция, строка формул
- **Сортировка**: сортировка, фильтр
- **Масштаб**: увеличить, уменьшить

#### События
- `zoom-change` — изменение масштаба
- `format-change` — изменение форматирования
- `ribbon-action` — действие с панели

### BaseComponent

**Файл**: `ui/core/component.ts`

#### Базовый класс для всех компонентов

```typescript
abstract class BaseComponent {
  protected container: HTMLElement;
  
  abstract init(): void;
  protected getElement<T>(id: string): T | null;
  protected querySelector<T>(selector: string): T | null;
  protected bindEvent(element, event, handler): void;
  destroy(): void;
}
```

---

## 🔧 Исправления и улучшения

### Производительность
- ✅ **Оптимизация рендеринга** ячеек
- ✅ **Ленивая загрузка** компонентов
- ✅ **Кэширование данных** для ИИ
- ✅ **Debouncing** для частых событий

### Безопасность
- ✅ **Content Security Policy** (CSP)
- ✅ **Context Isolation** в Electron
- ✅ **Node Integration** отключен
- ✅ **Preload скрипт** для безопасного IPC

### UX улучшения
- ✅ **Плавные анимации** панелей
- ✅ **Transition эффекты** для кнопок
- ✅ **Hover состояния** для интерактивных элементов
- ✅ **Кастомные скроллбары**
- ✅ **Адаптивный дизайн**

### Разработка
- ✅ **TypeScript** для типобезопасности
- ✅ **Модульная архитектура** для масштабируемости
- ✅ **Разделение ответственности** между компонентами
- ✅ **Легкость тестирования** отдельных модулей

---

## 📦 Зависимости

### Основные
- `electron` ^35.2.0
- `typescript` ^5.8.2
- `node-fetch` ^3.3.2

### Dev
- `@types/node` ^22.13.10
- `rimraf` ^5.0.0

---

## 🔮 Планы на будущее

### Версия 2.1.0
- [ ] **Совместная работа** в реальном времени
- [ ] **Облачная синхронизация** файлов
- [ ] **Шаблоны таблиц**
- [ ] **Макросы и скрипты**

### Версия 2.2.0
- [ ] **Расширенная аналитика** данных
- [ ] **Интеграция с внешними API**
- [ ] **Пользовательские функции**
- [ ] **Экспорт в PDF**

---

<div align="center">

**SmartTable v2.0.0** — Февраль 2025

*Создано с ❤️ для удобной работы с данными*

</div>
