# Руководство: Система шаблонов SmartTable

## Обзор

Система шаблонов в SmartTable позволяет:
- 📋 **Быстро создавать** таблицы со стандартной структурой
- 📤 **Экспортировать** созданные шаблоны для обмена с коллегами
- 📥 **Импортировать** шаблоны от других пользователей
- 🎯 **Применять** шаблоны для создания новых таблиц
- ⚙️ **Управлять** встроенными и пользовательскими шаблонами

---

## Структура системы

### Директории шаблонов

```
SmartTable/
├── templates/              # 📦 Встроенные шаблоны (поставляются с приложением)
│   ├── Контакты.json
│   ├── Продажи.json
│   └── Товары.json
│
└── user_templates/         # 👤 Пользовательские шаблоны (созданные вами)
    ├── templates/          # Основные файлы шаблонов
    ├── metadata/           # Метаданные для галереи
    └── thumbnails/         # Превью шаблонов
```

### Встроенные шаблоны

Поставляются с приложением:

1. **Контакты** - для ведения списков контактов
2. **Продажи** - для отчётов о продажах
3. **Товары** - для каталога товаров

---

## Использование галереи шаблонов

### Открыть галерею

**Способ 1: Через меню**
```
Инструменты → Галерея шаблонов...
```

**Способ 2: Горячая клавиша** (если добавлена в настройках)
```
Ctrl+Shift+L
```

### Интерфейс галереи

```
┌─────────────────────────────────────────────────┐
│ Галерея шаблонов          🔍 [Поиск .......] │
├──────────────────┬────────────────────────────┤
│ 📋 Контакты      │ Информация о шаблоне:      │
│ 📋 Продажи       │                              │
│ 📋 Товары        │ 📋 Контакты                │
│ 📋 Финансы       │ 📝 Управление контактами   │
│                  │                              │
│ (дополнительные) │ 📊 Структура (3 полей):    │
│                  │  1. Имя [text]             │
│                  │  2. Email [email]          │
│                  │  3. Телефон [phone]        │
│                  │                              │
├──────────────────┴────────────────────────────┤
│ 📥 Импортировать  📤 Экспортировать          │
│                              [✓ Создать таблицу] │
└─────────────────────────────────────────────────┘
```

### Применить шаблон (создать таблицу)

1. Откройте **Галерея шаблонов** через меню
2. Найдите нужный шаблон в списке слева
3. Просмотрите структуру в информационной панели справа
4. Нажмите **"✓ Создать таблицу"**
5. Будет создана новая таблица со структурой из шаблона

### Поиск шаблонов

1. Введите текст в поле поиска **🔍**
2. Список автоматически отфильтруется
3. Запрос ищет в названиях и описаниях

Примеры:
```
финанс    → найдет все шаблоны со словом "финанс"
контакт   → контакты компании, контакты клиентов
отчет     → все отчеты
```

---

## Создание собственного шаблона

### Способ 1: Через UI приложения (рекомендуется)

1. Создайте таблицу с нужной структурой
2. Заполните заголовки колонок
3. **Инструменты** → **Создать шаблон из выделения**
4. Заполните форму:
   - **Название** - уникальное имя шаблона
   - **Описание** - краткое описание назначения
   - **Типы данных** - выберите для каждого поля
   - **Ключевые поля** - отметьте уникальные поля
5. Нажмите **"Создать шаблон"**

### Способ 2: Вручную через JSON

1. Создайте файл `Мой_шаблон.json` в папке `user_templates/templates/`
2. Используйте следующий шаблон:

```json
{
  "name": "Мой шаблон",
  "description": "Описание назначения шаблона",
  "created_at": "2026-02-09T12:00:00.000000",
  "modified_at": "2026-02-09T12:00:00.000000",
  "source_range": null,
  "fields": [
    {
      "name": "Имя поля",
      "column_index": 0,
      "pattern": "text",
      "format_string": "",
      "is_key_field": false,
      "validation_rules": []
    },
    {
      "name": "Email",
      "column_index": 1,
      "pattern": "email",
      "format_string": "",
      "is_key_field": false,
      "validation_rules": []
    }
  ],
  "logic_rules": [],
  "settings": {
    "auto_detect_patterns": true,
    "preserve_formulas": true,
    "include_headers": true,
    "skip_empty_rows": true
  }
}
```

3. Создайте метаданные в `user_templates/metadata/Мой_шаблон.json`:

```json
{
  "name": "Мой шаблон",
  "description": "Описание назначения шаблона",
  "created_at": "2026-02-09T12:00:00.000000",
  "fields_count": 2,
  "tags": ["тег1", "тег2"]
}
```

---

## Типы данных полей

Доступные типы при создании шаблона:

| Тип | Код | Пример | Использование |
|-----|-----|--------|---------------|
| Текст | `text` | "Иван Петров" | Имена, описания |
| Число | `number` | 123, 45.67 | Количество, ID |
| Валюта | `currency` | 1000₽ | Цены, суммы |
| Дата | `date` | 2026-02-09 | Даты, графики |
| Email | `email` | user@example.com | Контакты |
| Телефон | `phone` | +7 (999) 123-45-67 | Контакты |
| URL | `url` | https://example.com | Веб-адреса |
| Процент | `percentage` | 50% | Доли, скидки |
| Формула | `formula` | =SUM(A:A) | Расчеты |

---

## Управление шаблонами

### Импортировать шаблон

1. Откройте **Галерея шаблонов**
2. Нажмите кнопку **"📥 Импортировать"**
3. Выберите JSON-файл шаблона
4. Шаблон будет добавлен в список

### Экспортировать шаблон

1. Откройте **Галерея шаблонов**
2. Выберите нужный шаблон
3. Нажмите кнопку **"📤 Экспортировать"**
4. Выберите папку для сохранения
5. Файл сохранится с расширением `.json`

### Переименовать шаблон

Способ 1 (простой):
1. Отредактируйте имя файла напрямую в папке
2. Обновите поле `name` внутри JSON файла

Способ 2 (через UI):
1. Сохраните шаблон с новым именем
2. Удалите старый

### Удалить шаблон

1. Откройте папку `user_templates/templates/`
2. Удалите JSON-файл
3. Удалите соответствующий файл из `user_templates/metadata/`

---

## Способы использования шаблонов

### Сценарий 1: Командная работа

```
Alice (сотрудник)
  1. Создает шаблон "Проверка качества"
  2. Экспортирует в файл
  3. Отправляет colleagues@company.com
  
Bob (сотрудник)
  1. Получает файл "Проверка качества.json"
  2. Открывает Галерею шаблонов
  3. Нажимает Импортировать
  4. Выбирает файл
  5. Сразу может использовать шаблон
  
Result: Вся команда использует единую структуру данных ✓
```

### Сценарий 2: Стандартизация процессов

```
Отдел продаж хочет унифицировать отчеты:
  1. Создается базовый шаблон "Ежедневный отчет"
  2. Менеджер распространяет всем продавцам
  3. Каждый продавец импортирует шаблон
  4. Все отчеты имеют единую структуру
```

### Сценарий 3: Переиспользование структуры

```
Каждый месяц нужна таблица с одинаковой структурой:
  1. Один раз создаешь шаблон
  2. Каждый месяц применяешь шаблон → новая таблица
  3. Заполняешь данные
  4. Сохраняешь в архив
```

---

## Примеры готовых шаблонов

### 📞 Контакты компании

**Структура:**
- Фамилия (text)
- Имя (text)
- Email (email)
- Телефон (phone)
- Должность (text)
- Компания (text)

**Использование:**
```
Управление контактами сотрудников, партнеров, клиентов
```

### 💰 Финансовый отчет

**Структура:**
- Дата (date)
- Статья (text)
- Категория (text)
- Доход (currency)
- Расход (currency)
- Баланс (currency)

**Использование:**
```
Анализ финансов, учет доходов и расходов
```

### 📦 Инвентаризация

**Структура:**
- Артикул (text, ключевое)
- Название (text)
- Категория (text)
- Количество (number)
- Единица (text)
- Цена закупки (currency)
- Цена продажи (currency)
- Дата обновления (date)

**Использование:**
```
Учет товаров на складе, контроль запасов
```

---

## Лучшие практики

### ✅ Делайте так:

1. **Давайте понятные названия**
   ```
   ✓ "Контакты_клиентов"
   ✓ "Отчет_продаж_декабрь"
   ✗ "template1", "test"
   ```

2. **Добавляйте подробное описание**
   ```
   Хорошо: "Шаблон для учета контактов с полями 
           для имени, email и телефона. 
           Используется отделом продаж"
   ```

3. **Используйте правильные типы данных**
   ```
   Email → pattern: "email"
   Цена → pattern: "currency"
   Дата → pattern: "date"
   ```

4. **Отмечайте ключевые поля**
   ```
   Артикул → is_key_field: true
   ID товара → is_key_field: true
   ```

5. **Организуйте полей логично**
   ```
   1. ID/Артикул (ключевое)
   2. Основная информация (название, описание)
   3. Контактные данные (email, телефон)
   4. Финансовые данные (цена, сумма)
   5. Метаинформация (дата, статус)
   ```

### ❌ Не делайте так:

1. Не создавайте шаблоны с одним полем
2. Не смешивайте разные типы данных в одном поле
3. Не забывайте обновлять метаданные
4. Не используйте специальные символы в названиях (`* ? / \ |`)

---

## Связь с другими функциями

### Печать таблицы из шаблона

```
1. Примените шаблон → создана таблица
2. Заполните данные
3. Файл → Печать (Ctrl+P)
4. Настройте параметры
5. Печать
```

### Экспорт таблицы из шаблона

```
1. Примените шаблон → создана таблица
2. Заполните данные
3. Файл → Экспортировать (Ctrl+E)
4. Выберите формат (Excel, CSV, JSON и т.д.)
5. Сохраните файл
```

### Применение форматирования к шаблону

```
1. Создайте шаблон
2. Примените его
3. Отформатируйте таблицу (цвета, шрифты, выравнивание)
4. Сохраните эту таблицу
5. Используйте как образец для будущих таблиц
```

---

## Решение проблем

### Проблема: Шаблон не загружается

**Решение:**
```
1. Проверьте синтаксис JSON (используйте https://jsonlint.com/)
2. Убедитесь, что файл в правильной кодировке (UTF-8)
3. Переместите файл в правильную папку:
   - Встроенные: templates/
   - Пользовательские: user_templates/templates/
4. Перезагрузите приложение
```

### Проблема: Типы данных неправильные

**Решение:**
```
1. Проверьте значение "pattern" в JSON:
   "pattern": "email"     ✓ правильно
   "pattern": "e-mail"    ✗ неправильно
2. Используйте только значения из таблицы выше
3. Пересохраните шаблон
```

### Проблема: Шаблон не появляется в галерее

**Решение:**
```
1. Проверьте расположение файлов
2. Убедитесь в наличии метаданных
3. Перезагрузите приложение
4. Проверьте лог ошибок консоли
```

### Проблема: Не могу открыть диалог галереи

**Решение:**
```
1. Инструменты → Галерея шаблонов
2. Если не работает, проверьте консоль на ошибки
3. Возможно нужно переустановить модули Python
```

---

## Версия и совместимость

- **Версия:** 1.0
- **Дата:** Февраль 2026
- **Совместимость:** SmartTable v1.0+
- **Python:** 3.8+

---

## Полезные ссылки

- [Документация по типам данных](../pysheets/user_templates/README.md)
- [Документация по темам оформления](../pysheets/user_themes/README.md)
- [Основное руководство](../md/README.md)

---

**Нужна помощь?** Обратитесь к документации или проверьте примеры шаблонов в папке `user_templates/`.
